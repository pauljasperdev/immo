{
  "name": "immo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/functions/*"
  ],
  "scripts": {
    "check": "biome check --write .",
    "dev": "pnpm sst dev",
    "dev:ios": "pnpm --filter native ios",
    "check-types": "pnpm --recursive check-types",
    "env:echoServerUrl": "sst shell -- pnpm --filter scripts env:echoServerUrl",
    "db:push": "sst shell -- pnpm --filter @immo/core db:push",
    "db:studio": "sst shell -- pnpm --filter @immo/core db:studio",
    "db:generate": "sst shell -- pnpm --filter @immo/core db:generate",
    "db:migrate": "sst shell -- pnpm --filter @immo/core db:migrate",
    "auth:generate": "pnpm --filter @immo/core auth:generate",
    "build:pre:local": "cd apps/native && eas build --profile preview-simulator --platform ios --local  --output ./build/preview-simulator.tar.gz && cd -",
    "build:dev:local": "cd apps/native && eas build --profile development-simulator --platform ios --local  --output ./build/development-simulator.tar.gz && cd -",
    "build:pre": "cd apps/native && eas build --profile preview --platform ios --non-interactive --no-wait && cd -",
    "prebuild": "cd apps/native && npx expo prebuild --clean --platform ios && cd -"
  },
  "dependencies": {
    "sst": "3.17.24"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "ultracite": "5.1.1"
  },
  "packageManager": "pnpm@10.15.0",
  "pnpm": {
    "overrides": {
      "glob@^10.0.0": ">=10.5.0"
    }
  }
}
